@model ApplicationUser

@if (Model.Image != null)
{
    <img src="/ViewImage/@($"{Model.Id}_{DateTime.Now.Millisecond}")" class="profilePic" />
    
}
else
{
    <img src="~/images/QM.jpg" class="profilePic" />
}

